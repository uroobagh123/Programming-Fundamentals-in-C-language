// This program calculates the area of a rectangle using given length and width.

#include <stdio.h>

int main() {
    int l = 2, w = 2, A;
    A = l * w;
    printf("Area of rectangle A = %d", A);
    return 0;
}
